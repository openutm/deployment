apiVersion: v1
kind: Service
metadata:
  name: redis-flight-blender
  labels:
    app: redis-flight-blender
    service: redis-flight-blender
spec:
  type: ClusterIP
  selector:
    app: redis-flight-blender
  ports:
    - name: redis
      port: 6379
      targetPort: redis
