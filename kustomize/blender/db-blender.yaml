apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: db-flight-blender
  name: db-flight-blender
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-flight-blender
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/blender-default: "true"
        app: db-flight-blender
    spec:
      containers:
        - name: db-flight-blender
          image: postgres
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: postgres
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-flight-blender-data
          env:
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
            - name: POSTGRES_HOST
              value: "db-flight-blender"
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_DB
                  name: env-secrets-blender
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_USER
                  name: env-secrets-blender
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_PASSWORD
                  name: env-secrets-blender
      restartPolicy: Always
      volumes:
        - name: db-flight-blender-data
          persistentVolumeClaim:
            claimName: db-flight-blender-data
status: {}
