apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: db-argon-server-data
  name: db-argon-server-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
